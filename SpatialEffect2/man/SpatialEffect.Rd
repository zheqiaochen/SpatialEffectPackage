% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SpatialEffect.R
\name{SpatialEffect}
\alias{SpatialEffect}
\title{Estimate the Average Marginalized Effect (AME) Curve}
\usage{
SpatialEffect(
  ras = NULL,
  Ydata = NULL,
  outcome = NULL,
  x_coord_Y = NULL,
  y_coord_Y = NULL,
  ras_Z = NULL,
  Zdata = NULL,
  x_coord_Z = NULL,
  y_coord_Z = NULL,
  treatment,
  covs = NULL,
  prob_treatment = NULL,
  dVec,
  dist.metric = "Euclidean",
  cType = "edge",
  numpts = NULL,
  evalpts = 1,
  only.unique = 0,
  smooth = 0,
  bw = NULL,
  bw_debias = NULL,
  bias_correction = TRUE,
  smooth.conley.se = 1,
  conf.band = 0,
  per.se = 1,
  blockvar = NULL,
  clustvar = NULL,
  conley.se = 1,
  kernel = "uni",
  cutoff = 0,
  alpha = 0.05,
  edf = FALSE,
  m = 2,
  lambda = 0.02,
  nPerms = 1000,
  n_threads = 1L
)
}
\arguments{
\item{ras}{A \code{\link[terra]{SpatRaster}} of the outcome surface, or NULL
to use kriging interpolation.}

\item{Ydata}{Data frame of outcome observations (used with kriging when
outcome locations differ from intervention nodes).}

\item{outcome}{Character name of the outcome variable column.}

\item{x_coord_Y, y_coord_Y}{Character names of coordinate columns in Ydata.}

\item{ras_Z}{An \code{\link[sf]{sf}} object for polygon interventions, or
NULL for point interventions. Can also be a SpatRaster or SpatVector which
will be converted to sf.}

\item{Zdata}{Data frame of intervention node attributes.}

\item{x_coord_Z, y_coord_Z}{Character names of coordinate columns in Zdata
(required for point interventions).}

\item{treatment}{Character name of the binary treatment column in Zdata.}

\item{covs}{Optional matrix of covariates (nz rows or nz*length(dVec) rows).}

\item{prob_treatment}{Character name of propensity score column in Zdata for
IPW (observational studies).}

\item{dVec}{Numeric vector of distance values at which to evaluate the AME.}

\item{dist.metric}{"Euclidean" (default) or "Geodesic".}

\item{cType}{Circle type: "edge" (default), "disk", or "donut".}

\item{numpts}{Number of sampling points on each circle (auto-selected if NULL).}

\item{evalpts}{Multiplier for auto-selected numpts (default 1).}

\item{only.unique}{Deduplicate raster cells per node (0 or 1).}

\item{smooth}{Whether to apply local polynomial smoothing (0 or 1).}

\item{bw}{Bandwidth for smoothing (auto-selected via CV if NULL).}

\item{bw_debias}{Bandwidth for bias correction (auto-selected if NULL).}

\item{bias_correction}{Use bias-corrected smoothing (default TRUE).}

\item{smooth.conley.se}{Compute Conley SE for smoothed estimates (0 or 1).}

\item{conf.band}{Compute uniform confidence bands for smoothed AME (0 or 1).}

\item{per.se}{Compute permutation-based CIs (0 or 1, default 1).}

\item{blockvar}{Block variable for stratified permutation.}

\item{clustvar}{Cluster variable for clustered permutation.}

\item{conley.se}{Compute Conley spatial HAC SEs (0 or 1, default 1).}

\item{kernel}{Kernel for HAC: "uni"/"uniform", "tri"/"triangular",
"epa"/"epanechnikov".}

\item{cutoff}{Spatial bandwidth for Conley SE (0 = EHW).}

\item{alpha}{Significance level (default 0.05).}

\item{edf}{Use effective degrees of freedom adjustment (default FALSE).}

\item{m}{Polynomial order for kriging (default 2).}

\item{lambda}{Smoothing parameter for kriging (default 0.02).}

\item{nPerms}{Number of permutations (default 1000).}

\item{n_threads}{Number of threads for parallel C++ computation (default 1).}
}
\value{
An S3 object of class "SpatialEffect" containing:
\describe{
  \item{AMR_est}{Data frame of distance and AME estimates}
  \item{Per.CI}{Permutation confidence intervals (if per.se=1)}
  \item{Conley.SE}{Conley standard errors (if conley.se=1)}
  \item{Conley.CI}{Conley confidence intervals (if conley.se=1)}
  \item{AMR_est_smoothed}{Smoothed estimates (if smooth=1)}
  \item{smoothed.Conley.SE}{Smoothed Conley SEs (if smooth=1 and smooth.conley.se=1)}
  \item{smoothed.Conley.CI}{Smoothed Conley CIs}
  \item{smoothed.Conley.CB}{Uniform confidence bands (if conf.band=1)}
  \item{Parameters}{List of stored parameters for downstream use}
}
}
\description{
Estimates how treatment effects vary with distance in space for spatial
experiments under unknown interference, following Wang, Samii, Chang and
Aronow (2024).
}
